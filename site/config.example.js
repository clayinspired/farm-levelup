// Copy to site/config.js and fill in your own deployment.
// site/config.js is gitignored: it is the only place the live hostnames live.
window.FARM = {
  store: "https://your-store.example.com",   // the service (bot + plot store)
  bot:   "https://t.me/your_bot"             // Telegram bot link on the landing page
};
